#include "../../include/models.h"
#include <stddef.h> // Добавляем эту строку

User *user_create(const char *username, const char *password_hash)
{
    return NULL; // заглушка
}

void user_free(User *user)
{
    // заглушка
}