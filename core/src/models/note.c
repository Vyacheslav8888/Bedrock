#include "../../include/models.h"
#include <stddef.h> // Добавляем эту строку

Note *note_create(const char *title, const char *content)
{
    return NULL; // заглушка
}

void note_free(Note *note)
{
    // заглушка
}