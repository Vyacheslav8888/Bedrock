#include "../../include/crypto.h"
#include <string.h>

char *hash_password(const char *password)
{
    return "hashed_password"; // временная заглушка
}

int verify_password(const char *password, const char *hash)
{
    return 1; // временная заглушка
}